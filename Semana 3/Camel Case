#include <stdio.h>
int main()
{
    char str[100001];
    scanf("%s", str);
    int word=1;
    for(int i=0; str[i]!='\0'; i++)
        if(str[i]<91)   word++;
    printf("%d\n", word);
    return 0;
}
